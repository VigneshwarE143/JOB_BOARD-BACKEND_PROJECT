# ------------------------------
# APPLICATION CONFIGURATION
# ------------------------------
spring.application.name=jobboard-backend
server.port=8080

# ------------------------------
# DATABASE CONFIGURATION (MySQL)
# ------------------------------
spring.datasource.url=jdbc:mysql://localhost:3306/job_house?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=05012005
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate / JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ------------------------------
# JWT CONFIGURATION
# ------------------------------
app.jwtSecret=myverysecretkeyforjwtgenerationmustbeatleast32chars
app.jwtExpirationMs=86400000

# ------------------------------
# LOGGING
# ------------------------------
logging.level.root=INFO
logging.level.org.springframework.security=INFO
